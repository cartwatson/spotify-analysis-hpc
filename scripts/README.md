# Running instructions for scripts

## py_install

1. run `./scripts/py_install.sh` from the root dir of the project  
2. to reactivate the `.venv` run `source .venv/bin/activate` from the root dir
3. run `deactivate` to deactivate the venv

## build.sh

to be added later
